package geotechnicalapp.geotech.domain.artefacts;

import org.springframework.data.jpa.repository.JpaRepository;

public interface GeoTechArtifactsRepo extends JpaRepository<GeoTechArtifacts,Long> {
}
